keys can be found in ~/.ssh/
they can be generated by using the command ssh-keygen -t rsa -b 4096 -C "youremail@something.com"
-b is for bits, default is set at 2048.
id_rsa is private key
by doing cat ~/.ssh/id_rsa the result will be the private key
id_rsa.pub in the same folder is the public key
You can install key by using this link : https://support.rackspace.com/how-to/logging-in-with-an-ssh-private-key-on-linuxmac/
For authorize keys can be located in same folder as authorized_keys
